<Page x:Class="SystemCare.Views.StartupPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:helpers="clr-namespace:SystemCare.Helpers"
      xmlns:converters="clr-namespace:SystemCare.Converters"
      xmlns:controls="clr-namespace:SystemCare.Controls"
      xmlns:vm="clr-namespace:SystemCare.ViewModels"
      mc:Ignorable="d"
      TextElement.Foreground="{StaticResource TextFillColorPrimaryBrush}"
      d:DataContext="{d:DesignInstance Type=vm:StartupViewModel}"
      Title="Startup"
      Loaded="OnPageLoaded">
    <Page.Resources>
        <converters:BoolToVisibilityConverter x:Key="VisibleWhenTrue" />
    </Page.Resources>

    <Grid Margin="24,12,24,24" helpers:Animations.StaggerChildren="True">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock Text="Startup Manager" Style="{StaticResource CyberPageTitle}" />
            <TextBlock Text="Programs and tasks that launch when Windows starts. Disabling keeps the entry so you can re-enable it later."
                       Style="{StaticResource TextBody}" Margin="0,4,0,0" />

            <ui:Card Margin="0,12,0,0" Padding="14,10">
                <StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <ui:SymbolIcon Symbol="Timer24" FontSize="16" Foreground="{StaticResource NeonCyanBrush}" VerticalAlignment="Center" Margin="0,0,8,0" />
                        <TextBlock Text="Boot performance" FontWeight="SemiBold" VerticalAlignment="Center" />
                        <TextBlock Text="{Binding BootSummary}" FontSize="12" Foreground="{StaticResource TextFillColorTertiaryBrush}" VerticalAlignment="Center" Margin="10,0,0,0" TextTrimming="CharacterEllipsis" />
                    </StackPanel>
                    <ItemsControl ItemsSource="{Binding BootApps}" Margin="24,8,0,0"
                                  Visibility="{Binding HasBootApps, Converter={StaticResource VisibleWhenTrue}}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate><WrapPanel /></ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Margin="0,0,8,8" Padding="9,4" CornerRadius="4" Background="{StaticResource PanelInsetBrush}"
                                        BorderBrush="{StaticResource CyberStrokeBrush}" BorderThickness="1">
                                    <StackPanel Orientation="Horizontal">
                                        <Ellipse Width="8" Height="8" Fill="{Binding LevelBrush}" VerticalAlignment="Center" Margin="0,0,6,0" />
                                        <TextBlock Text="{Binding Name}" FontSize="12" VerticalAlignment="Center" />
                                        <TextBlock Text="{Binding DurationText}" FontSize="12" FontWeight="SemiBold"
                                                   VerticalAlignment="Center" Margin="8,0,0,0" Foreground="{Binding LevelBrush}" />
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </ui:Card>
        </StackPanel>

        <Grid Grid.Row="1" Margin="0,14,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <ui:Button Grid.Column="0" Content="Refresh" Command="{Binding RefreshCommand}" Padding="18,7">
                <ui:Button.Icon>
                    <ui:SymbolIcon Symbol="ArrowClockwise24" />
                </ui:Button.Icon>
            </ui:Button>
            <ComboBox Grid.Column="1" Margin="10,0,0,0" MinWidth="130"
                      SelectedIndex="{Binding FilterIndex}">
                <ComboBoxItem Content="All items" />
                <ComboBoxItem Content="Enabled" />
                <ComboBoxItem Content="Disabled" />
            </ComboBox>
            <ui:TextBox Grid.Column="2" Margin="10,0" PlaceholderText="Search by name, publisher or command…"
                        Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                        Icon="{ui:SymbolIcon Search24}" />
            <CheckBox Grid.Column="3" Content="Show Windows system tasks"
                      IsChecked="{Binding ShowSystemTasks}" VerticalAlignment="Center" />
        </Grid>

        <ui:ListView Grid.Row="2" ItemsSource="{Binding Items}"
                     VirtualizingPanel.IsVirtualizing="True"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <ui:ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="0,6">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="170" />
                            <ColumnDefinition Width="150" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <ui:ToggleSwitch Grid.Column="0" IsChecked="{Binding IsEnabled}" VerticalAlignment="Center" />
                        <StackPanel Grid.Column="1" Margin="12,0,8,0" VerticalAlignment="Center">
                            <TextBlock Text="{Binding Name}" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" />
                            <TextBlock Text="{Binding Command}" FontSize="11" Foreground="{StaticResource TextFillColorTertiaryBrush}" TextTrimming="CharacterEllipsis" />
                        </StackPanel>
                        <TextBlock Grid.Column="2" Text="{Binding Publisher}" VerticalAlignment="Center"
                                   FontSize="12" Foreground="{StaticResource TextFillColorSecondaryBrush}" TextTrimming="CharacterEllipsis" />
                        <TextBlock Grid.Column="3" Text="{Binding SourceDisplay}" VerticalAlignment="Center"
                                   Style="{StaticResource TextCaption}" />
                        <ui:Button Grid.Column="4" Appearance="Danger" Padding="8,6" ToolTip="Delete entry"
                                   Command="{Binding DataContext.DeleteCommand, RelativeSource={RelativeSource AncestorType=Page}}"
                                   CommandParameter="{Binding}">
                            <ui:Button.Icon>
                                <ui:SymbolIcon Symbol="Delete24" />
                            </ui:Button.Icon>
                        </ui:Button>
                    </Grid>
                </DataTemplate>
            </ui:ListView.ItemTemplate>
        </ui:ListView>

        <StackPanel Grid.Row="3" Margin="0,10,0,0">
            <controls:TaskProgress IsActive="{Binding IsBusy}" />
            <TextBlock Text="{Binding SummaryText}" Style="{StaticResource TextCaption}" Margin="0,6,0,0" />
        </StackPanel>
    </Grid>
</Page>
