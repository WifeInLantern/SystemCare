<Page x:Class="SystemCare.Views.WifiAnalyzerPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:helpers="clr-namespace:SystemCare.Helpers"
      xmlns:controls="clr-namespace:SystemCare.Controls"
      xmlns:converters="clr-namespace:SystemCare.Converters"
      xmlns:vm="clr-namespace:SystemCare.ViewModels"
      mc:Ignorable="d"
      TextElement.Foreground="{StaticResource TextFillColorPrimaryBrush}"
      d:DataContext="{d:DesignInstance Type=vm:WifiAnalyzerViewModel}"
      Title="Wi-Fi Analyzer"
      Loaded="OnPageLoaded">
    <Page.Resources>
        <converters:BoolToVisibilityConverter x:Key="VisibleWhenTrue" />
    </Page.Resources>

    <Grid Margin="24,12,24,20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock Text="Wi-Fi Analyzer" Style="{StaticResource CyberPageTitle}" />
            <TextBlock Text="Your connection's real signal, channel and link rate — plus every network in range, so you can spot congestion and dead spots."
                       Style="{StaticResource TextBody}" Margin="0,4,0,0" />
        </StackPanel>

        <!-- Current connection -->
        <ui:Card Grid.Row="1" Margin="0,14,0,0" Padding="18,14">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                    <StackPanel Orientation="Horizontal" Visibility="{Binding IsConnected, Converter={StaticResource VisibleWhenTrue}}">
                        <ui:SymbolIcon Symbol="Wifi124" FontSize="20" Foreground="{StaticResource NeonCyanBrush}" VerticalAlignment="Center" />
                        <TextBlock Text="{Binding Ssid}" Style="{StaticResource TextH2}" Margin="10,0,0,0" VerticalAlignment="Center" />
                        <Border Style="{StaticResource CyberChipInfo}" Margin="12,0,0,0" VerticalAlignment="Center">
                            <TextBlock Style="{StaticResource ChipTextInfo}">
                                <Run Text="{Binding SignalPercent, Mode=OneWay}" /><Run Text="% signal" />
                            </TextBlock>
                        </Border>
                    </StackPanel>
                    <TextBlock Text="{Binding ConnectionDetail}" Style="{StaticResource TextCaption}" Margin="0,6,0,0"
                               Visibility="{Binding IsConnected, Converter={StaticResource VisibleWhenTrue}}" />
                    <TextBlock Text="{Binding CongestionText}" Style="{StaticResource TextCaption}" Margin="0,4,0,0" TextWrapping="Wrap"
                               Visibility="{Binding IsConnected, Converter={StaticResource VisibleWhenTrue}}" />
                    <TextBlock Text="{Binding StatusText}" Style="{StaticResource TextCaption}" Margin="0,6,0,0" TextWrapping="Wrap" />
                </StackPanel>
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Top">
                    <controls:TaskProgress IsActive="{Binding IsBusy}" VerticalAlignment="Center" Margin="0,0,10,0" />
                    <ui:Button Content="Refresh" Appearance="Primary" Padding="14,7" Command="{Binding RefreshCommand}">
                        <ui:Button.Icon><ui:SymbolIcon Symbol="ArrowSync24" /></ui:Button.Icon>
                    </ui:Button>
                </StackPanel>
            </Grid>
        </ui:Card>

        <!-- Nearby networks -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" Margin="0,12,0,0">
            <ItemsControl ItemsSource="{Binding Nearby}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{StaticResource CyberListRow}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="Auto" MinWidth="64" />
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="{Binding Ssid}" Style="{StaticResource TextBodyStrong}"
                                           VerticalAlignment="Center" TextTrimming="CharacterEllipsis" />
                                <TextBlock Grid.Column="1" Text="{Binding DetailText}" Style="{StaticResource TextCaption}"
                                           VerticalAlignment="Center" Margin="10,0" />
                                <TextBlock Grid.Column="2" Text="{Binding SignalText}" Style="{StaticResource TextMetricValue}"
                                           VerticalAlignment="Center" />
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</Page>
