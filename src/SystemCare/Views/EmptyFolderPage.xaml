<Page x:Class="SystemCare.Views.EmptyFolderPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:converters="clr-namespace:SystemCare.Converters"
      xmlns:vm="clr-namespace:SystemCare.ViewModels"
      mc:Ignorable="d"
      d:DataContext="{d:DesignInstance Type=vm:EmptyFolderViewModel}"
      Title="Empty Folders">
    <Page.Resources>
        <converters:BoolToVisibilityConverter x:Key="VisibleWhenTrue" />
        <converters:InverseBoolConverter x:Key="Not" />
    </Page.Resources>

    <Grid Margin="24,12,24,24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock Text="Empty Folder Finder" FontSize="24" FontWeight="SemiBold" />
            <TextBlock Text="{Binding StatusText}" FontSize="13" Opacity="0.75" Margin="0,4,0,0" TextWrapping="Wrap" />
        </StackPanel>

        <Grid Grid.Row="1" Margin="0,14,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <ui:TextBox Grid.Column="0" Text="{Binding SelectedPath}" IsReadOnly="True" Icon="{ui:SymbolIcon Folder24}" />
            <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="8,0,0,0">
                <ui:Button Content="Browse…" Padding="14,7" Command="{Binding PickFolderCommand}" />
                <ui:Button Content="Scan" Appearance="Primary" Padding="22,7" Margin="6,0,0,0"
                           Command="{Binding ScanCommand}" IsEnabled="{Binding IsBusy, Converter={StaticResource Not}}">
                    <ui:Button.Icon><ui:SymbolIcon Symbol="Search24" /></ui:Button.Icon>
                </ui:Button>
                <ui:Button Content="Cancel" Padding="14,7" Margin="6,0,0,0"
                           Command="{Binding ScanCancelCommand}"
                           Visibility="{Binding IsBusy, Converter={StaticResource VisibleWhenTrue}}" />
            </StackPanel>
        </Grid>

        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding Folders}" VirtualizingPanel.IsVirtualizing="True">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <ui:Card Margin="0,0,0,5" Padding="12,8">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <CheckBox Grid.Column="0" IsChecked="{Binding IsSelected}" VerticalAlignment="Center" />
                                <TextBlock Grid.Column="1" Text="{Binding Path}" Margin="8,0,0,0" VerticalAlignment="Center"
                                           FontSize="12" TextTrimming="CharacterEllipsis" ToolTip="{Binding Path}" />
                            </Grid>
                        </ui:Card>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <StackPanel Grid.Row="3" Margin="0,10,0,0">
            <ProgressBar IsIndeterminate="True" Height="4"
                         Visibility="{Binding IsBusy, Converter={StaticResource VisibleWhenTrue}}" />
            <TextBlock Text="{Binding ProgressText}" FontSize="11" Opacity="0.55" Margin="0,6,0,8" TextTrimming="CharacterEllipsis" />
            <ui:Button Content="Remove selected (to Recycle Bin)" Appearance="Danger" Padding="20,8"
                       Command="{Binding RemoveCommand}">
                <ui:Button.Icon><ui:SymbolIcon Symbol="Delete24" /></ui:Button.Icon>
            </ui:Button>
        </StackPanel>
    </Grid>
</Page>
