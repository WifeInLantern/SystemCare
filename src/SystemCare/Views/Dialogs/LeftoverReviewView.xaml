<UserControl x:Class="SystemCare.Views.Dialogs.LeftoverReviewView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:vm="clr-namespace:SystemCare.ViewModels"
             mc:Ignorable="d"
             TextElement.Foreground="{StaticResource TextFillColorPrimaryBrush}"
             d:DataContext="{d:DesignInstance Type=vm:SoftwareUninstallerViewModel}"
             d:DesignWidth="560">
    <Grid Width="560" MaxHeight="420">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="{Binding HeaderText}" TextWrapping="Wrap"
                   Opacity="0.75" Margin="0,0,0,10" />

        <ui:ListView Grid.Row="1" ItemsSource="{Binding Leftovers}" SelectionMode="Single"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <ui:ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="0,4">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <CheckBox Grid.Column="0" VerticalAlignment="Center"
                                  IsChecked="{Binding IsSelected, Mode=TwoWay}" />
                        <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="8,0,8,0">
                            <TextBlock Text="{Binding DisplayPath}" FontWeight="SemiBold"
                                       TextTrimming="CharacterEllipsis" />
                            <TextBlock FontSize="11" Opacity="0.6" TextTrimming="CharacterEllipsis">
                                <Run Text="{Binding KindText, Mode=OneWay}" /><Run Text="  ·  " /><Run Text="{Binding Reason, Mode=OneWay}" />
                            </TextBlock>
                        </StackPanel>
                        <TextBlock Grid.Column="2" Text="{Binding SizeText}" VerticalAlignment="Center"
                                   FontSize="12" Opacity="0.8" />
                    </Grid>
                </DataTemplate>
            </ui:ListView.ItemTemplate>
        </ui:ListView>
    </Grid>
</UserControl>
