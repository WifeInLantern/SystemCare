<Page x:Class="SystemCare.Views.NetworkSecurityAuditPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:helpers="clr-namespace:SystemCare.Helpers"
      xmlns:vm="clr-namespace:SystemCare.ViewModels"
      mc:Ignorable="d"
      TextElement.Foreground="{StaticResource TextFillColorPrimaryBrush}"
      d:DataContext="{d:DesignInstance Type=vm:NetworkSecurityAuditViewModel}"
      Title="Network Security"
      Loaded="OnPageLoaded">

    <Grid Margin="24,12,24,24" helpers:Animations.StaggerChildren="True">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock Text="Network Security Audit" Style="{StaticResource CyberPageTitle}" />
            <TextBlock Text="{Binding SummaryText}" FontSize="13" Foreground="{StaticResource TextFillColorSecondaryBrush}" Margin="0,4,0,0" />
        </StackPanel>

        <ui:Button Grid.Row="1" Content="Refresh" Padding="18,8" Margin="0,14,0,0" Command="{Binding RefreshCommand}">
            <ui:Button.Icon>
                <ui:SymbolIcon Symbol="ArrowClockwise24" />
            </ui:Button.Icon>
        </ui:Button>

        <TabControl Grid.Row="2" Margin="0,12,0,0">
            <TabItem Header="Listening ports">
                <Grid Margin="0,12,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <Grid Grid.Row="0" Margin="8,0,8,4">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="60" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="90" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="80" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="Proto" FontSize="11" Foreground="{StaticResource TextFillColorTertiaryBrush}" />
                        <TextBlock Grid.Column="1" Text="Local address" FontSize="11" Foreground="{StaticResource TextFillColorTertiaryBrush}" />
                        <TextBlock Grid.Column="2" Text="Port" FontSize="11" Foreground="{StaticResource TextFillColorTertiaryBrush}" />
                        <TextBlock Grid.Column="3" Text="Process" FontSize="11" Foreground="{StaticResource TextFillColorTertiaryBrush}" />
                    </Grid>
                    <ui:ListView Grid.Row="1" ItemsSource="{Binding ListeningPorts}"
                                 VirtualizingPanel.IsVirtualizing="True" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                        <ui:ListView.ItemTemplate>
                            <DataTemplate>
                                <Grid Margin="0,4">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="60" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="90" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="80" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="{Binding Protocol}" FontSize="12" Foreground="{StaticResource TextFillColorTertiaryBrush}" VerticalAlignment="Center" />
                                    <TextBlock Grid.Column="1" Text="{Binding LocalAddress}" FontSize="12" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" />
                                    <TextBlock Grid.Column="2" Text="{Binding Port}" FontSize="12" Foreground="{StaticResource TextFillColorTertiaryBrush}" VerticalAlignment="Center" />
                                    <TextBlock Grid.Column="3" Text="{Binding ProcessName}" FontSize="12" FontWeight="SemiBold" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" />
                                    <ui:Button Grid.Column="4" Content="Block" Padding="10,4" FontSize="11"
                                               Appearance="Danger"
                                               Command="{Binding DataContext.BlockAppCommand, RelativeSource={RelativeSource AncestorType=Page}}"
                                               CommandParameter="{Binding}" />
                                </Grid>
                            </DataTemplate>
                        </ui:ListView.ItemTemplate>
                    </ui:ListView>
                </Grid>
            </TabItem>

            <TabItem Header="Blocked apps">
                <ui:ListView Margin="0,12,0,0" ItemsSource="{Binding BlockedApps}"
                             VirtualizingPanel.IsVirtualizing="True" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                    <ui:ListView.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="0,4">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="2*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="{Binding DisplayName}" FontSize="13" FontWeight="SemiBold" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" />
                                <TextBlock Grid.Column="1" Text="{Binding ApplicationPath}" FontSize="11" Foreground="{StaticResource TextFillColorTertiaryBrush}" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" />
                                <ui:Button Grid.Column="2" Content="Unblock" Padding="10,4" FontSize="11"
                                           Command="{Binding DataContext.UnblockAppCommand, RelativeSource={RelativeSource AncestorType=Page}}"
                                           CommandParameter="{Binding}" />
                            </Grid>
                        </DataTemplate>
                    </ui:ListView.ItemTemplate>
                </ui:ListView>
            </TabItem>
        </TabControl>
    </Grid>
</Page>
