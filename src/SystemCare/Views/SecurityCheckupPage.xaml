<Page x:Class="SystemCare.Views.SecurityCheckupPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:converters="clr-namespace:SystemCare.Converters"
      xmlns:helpers="clr-namespace:SystemCare.Helpers"
      xmlns:vm="clr-namespace:SystemCare.ViewModels"
      mc:Ignorable="d"
      d:DataContext="{d:DesignInstance Type=vm:SecurityCheckupViewModel}"
      Title="Security"
      Loaded="OnPageLoaded">
    <Page.Resources>
        <converters:BoolToVisibilityConverter x:Key="VisibleWhenTrue" />
    </Page.Resources>

    <Grid Margin="24,12,24,24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock Text="Security Checkup" Style="{StaticResource CyberPageTitle}" />
            <TextBlock Text="{Binding SummaryText}" FontSize="13" Opacity="0.75" Margin="0,4,0,0" TextWrapping="Wrap" />
        </StackPanel>

        <ui:Button Grid.Row="1" Content="Re-scan" Padding="18,8" Margin="0,14,0,0" Command="{Binding ScanCommand}">
            <ui:Button.Icon>
                <ui:SymbolIcon Symbol="ArrowClockwise24" />
            </ui:Button.Icon>
        </ui:Button>

        <ProgressBar Grid.Row="2" IsIndeterminate="True" Height="4" Margin="0,12,0,0"
                     Visibility="{Binding IsBusy, Converter={StaticResource VisibleWhenTrue}}" />

        <ScrollViewer Grid.Row="3" VerticalScrollBarVisibility="Auto" Margin="0,12,0,0">
            <ItemsControl ItemsSource="{Binding Checks}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <ui:Card Margin="0,0,0,8" Padding="16,12" helpers:Animations.FadeInOnLoad="True">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <ui:SymbolIcon Grid.Column="0" Symbol="{Binding Icon}" FontSize="24" VerticalAlignment="Center" />
                                <StackPanel Grid.Column="1" Margin="12,0,12,0" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding Name}" FontWeight="SemiBold" />
                                    <TextBlock Text="{Binding Message}" FontSize="12" Opacity="0.65" TextWrapping="Wrap" />
                                </StackPanel>
                                <Border Grid.Column="2" CornerRadius="10" Padding="9,3" VerticalAlignment="Center" Margin="0,0,8,0">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="Background" Value="#33445566" />
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding StatusText}" Value="OK">
                                                    <Setter Property="Background" Value="#3300FFA3" />
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding StatusText}" Value="Review">
                                                    <Setter Property="Background" Value="#33FFD300" />
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding StatusText}" Value="At risk">
                                                    <Setter Property="Background" Value="#33FF2A6D" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <TextBlock Text="{Binding StatusText}" FontSize="11" FontWeight="SemiBold" />
                                </Border>
                                <ui:Button Grid.Column="3" Content="{Binding FixLabel}" Padding="12,6"
                                           Command="{Binding DataContext.FixCommand, RelativeSource={RelativeSource AncestorType=Page}}"
                                           CommandParameter="{Binding}">
                                    <ui:Button.Style>
                                        <Style TargetType="ui:Button" BasedOn="{StaticResource {x:Type ui:Button}}">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding FixTarget}" Value="{x:Null}">
                                                    <Setter Property="Visibility" Value="Collapsed" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </ui:Button.Style>
                                </ui:Button>
                            </Grid>
                        </ui:Card>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</Page>
