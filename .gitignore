# .NET build artifacts (regenerable — never commit)
bin/
obj/
*.user

# Visual Studio / VS Code
.vs/

# Built artifacts (exe + installer) — distributed via GitHub Releases, not committed.
dist/

# Per-release notes live in CHANGELOG.md and on the GitHub release pages.
RELEASE_NOTES_*.md

